import { GlyphLoading } from "@/components/glyph-loading";

/** Every route segment that has to wait says so in the site's own alphabet. */
export default function Loading() {
  return <GlyphLoading />;
}
